<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

  <form action="/payrolls/<%= foundPayroll.employee._id %>/<%= foundPayroll._id %>?_method=PUT" method="POST">

    <div>
      <label for="pay_date">Pay Date:</label>
      <input type="date" name="pay_date" id="pay_date" value="<%= foundPayroll.pay_date.toISOString().slice(0, 10) %>">
    </div>

    <div>
      <label for="hours_worked">Hours Worked:</label>
      <input type="number" name="hours_worked" id="hours_worked" value="<%= foundPayroll.hours_worked %>">
    </div>

    <div>
      <label for="bonus">Bonus:</label>
      <input type="number" name="bonus" id="bonus" value="<%= foundPayroll.bonus %>">
    </div>

    <button type="submit">Create Payroll</button>
  </form>
</body>
</html>