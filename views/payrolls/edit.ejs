
  <%- include('../partials/head') %>
  <div class="container">
    <h1>Edit Payroll for <%= foundPayroll.employee.name %></h1>

    <form action="/payrolls/<%= foundPayroll.employee._id %>/<%= foundPayroll._id %>?_method=PUT" method="POST" class="form-card">
      
      <div class="form-group">
        <label for="pay_date">Pay Date</label>
        <input type="date" name="pay_date" id="pay_date" value="<%= foundPayroll.pay_date.toISOString().slice(0, 10) %>" required>
      </div>

      <div class="form-group">
        <label for="hours_worked">Hours Worked</label>
        <input type="number" name="hours_worked" id="hours_worked" min="0" max="300" value="<%= foundPayroll.hours_worked %>" required>
      </div>

      <div class="form-group">
        <label for="bonus">Bonus (%)</label>
        <input type="number" name="bonus" id="bonus" min="0" max="100" value="<%= foundPayroll.bonus %>" required>
      </div>

      <button type="submit" class="btn">Save Changes</button>
      <a href="/payrolls/<%= foundPayroll.employee._id %>" class="btn btn-secondary">⬅️ Cancel</a>
    </form>
  </div>
</body>
</html>
