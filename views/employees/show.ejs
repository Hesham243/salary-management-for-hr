  <%- include('../partials/head') %>
  
  <div class="container">
    <h1>HR Manager: <%= foundEmployee.user.username %></h1>

    <div class="employee-card">
      <h2><%= foundEmployee.name %></h2>
      <p><strong>Position:</strong> <%= foundEmployee.position %></p>
      <p><strong>Salary:</strong> BHD <%= foundEmployee.salary %></p>
    </div>
    
    <% if (foundEmployee.user._id.equals(user._id)) { %>
      <div class="btn-group">
        <a href="/employees/<%= foundEmployee._id %>/edit" class="btn">✏️ Edit Employee</a>

        <form action="/employees/<%= foundEmployee._id %>?_method=DELETE" method="POST">
          <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
            DELETE
          </button>
          
          <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog">
              <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                  Are you sure you want to delete <%= foundEmployee.name %> from the directory employee
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                  <button type="submit" class="btn btn-primary btn-danger">🗑️ DELETE</button>
                </div>
              </div>
            </div>
          </div>
        </form>

        <a href="/payrolls/<%= foundEmployee._id %>/new" class="btn">Add Payroll</a>
        <a href="/payrolls/<%= foundEmployee._id %>" class="btn btn-secondary">View Payroll History</a>
      </div>
    <% } else { %>
      <p class="login-link">You do not have permission to manage this employee.</p>
    <% } %>
  </div>
</body>
</html>
